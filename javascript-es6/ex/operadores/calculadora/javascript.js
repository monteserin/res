const n1 = 3;
const n2 = 7;
const op = '*';

if (op === '+') {
	console.log(n1 + n2);
} else if (op === '-') {
	console.log(n1 - n2);
} else if (op === '/') {
	console.log(n1 / n2);
} else if (op === '*') {
	console.log(n1 * n2);
}